interface:
  display_name: "2a-project-architecture-gate"
  short_description: "Gate de arquitectura por capas"
  default_prompt: "Use $2a-project-architecture-gate to run strict layer-boundary checks and update docs/todo.md."

policy:
  allow_implicit_invocation: false
